# To make absolutely sure the framework doesn't crash from missing structures, let's use a standard Minecraft structure that definitely exists!
# "minecraft:gametest.empty" or similar. But if I don't provide a template, it enforces mineminenomi:examplegametest.empty
