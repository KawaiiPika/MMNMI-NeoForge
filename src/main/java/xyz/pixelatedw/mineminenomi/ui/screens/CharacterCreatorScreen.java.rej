--- src/main/java/xyz/pixelatedw/mineminenomi/ui/screens/CharacterCreatorScreen.java
+++ src/main/java/xyz/pixelatedw/mineminenomi/ui/screens/CharacterCreatorScreen.java
@@ -82,8 +82,8 @@
     @Override
     public void render(GuiGraphics graphics, int x, int y, float f) {
         this.renderBackground(graphics, x, y, f);
-        int centerX = this.width / 2;
-        int centerY = this.height / 2;
+        int centerX = graphics.guiWidth() / 2;
+        int centerY = graphics.guiHeight() / 2;

         graphics.pose().pushPose();
         graphics.pose().translate((float)(centerX - 150), (float)(centerY - 120), 0.0F);
@@ -107,8 +107,8 @@

     @Override
     public void init() {
-        int centerX = this.width / 2;
-        int centerY = this.height / 2;
+        int centerX = this.minecraft.getWindow().getGuiScaledWidth() / 2;
+        int centerY = this.minecraft.getWindow().getGuiScaledHeight() / 2;

         int buttonX = centerX - 110;
         int buttonY = centerY - 25;
