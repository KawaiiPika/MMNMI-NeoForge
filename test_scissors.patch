--- src/main/java/xyz/pixelatedw/mineminenomi/items/weapons/ScissorsItem.java
+++ src/main/java/xyz/pixelatedw/mineminenomi/items/weapons/ScissorsItem.java
@@ -2,10 +2,92 @@

+import java.util.List;
+import java.util.Random;
+
+import net.minecraft.core.BlockPos;
+import net.minecraft.tags.BlockTags;
+import net.minecraft.world.InteractionHand;
+import net.minecraft.world.InteractionResult;
+import net.minecraft.world.entity.EquipmentSlot;
+import net.minecraft.world.entity.LivingEntity;
+import net.minecraft.world.entity.item.ItemEntity;
+import net.minecraft.world.entity.player.Player;
+import net.minecraft.world.item.ItemStack;
+import net.minecraft.world.item.enchantment.Enchantments;
+import net.minecraft.world.level.Level;
+import net.minecraft.world.level.block.Block;
+import net.minecraft.world.level.block.Blocks;
+import net.minecraft.world.level.block.state.BlockState;
+import net.neoforged.neoforge.common.IForgeShearable;
+
+import xyz.pixelatedw.mineminenomi.data.entity.PlayerStats;
+import xyz.pixelatedw.mineminenomi.init.ModAbilities;
+import xyz.pixelatedw.mineminenomi.init.ModDataAttachments;
+import xyz.pixelatedw.mineminenomi.init.ModItems;
 import xyz.pixelatedw.mineminenomi.init.ModTiers;

 public class ScissorsItem extends ModSwordItem {
+
     public ScissorsItem() {
         super(ModTiers.SCISSORS, 5, -2.8F);
     }

-    // TODO: Implement shearing and shadow stealing logic
+    @Override
+    public boolean hurtEnemy(ItemStack stack, LivingEntity target, LivingEntity attacker) {
+        if (attacker instanceof Player playerAttacker) {
+            PlayerStats attackerStats = playerAttacker.getData(ModDataAttachments.PLAYER_STATS);
+
+            boolean hasPassive = attackerStats.getActiveAbilities().contains(ModAbilities.REGISTRY.getKey(ModAbilities.KAGE_GIRI.get()).toString());
+
+            if (target instanceof Player targetPlayer) {
+                PlayerStats targetStats = targetPlayer.getData(ModDataAttachments.PLAYER_STATS);
+                if (hasPassive && targetStats.hasShadow()) {
+                    targetStats.setHasShadow(false);
+                    targetStats.sync(targetPlayer);
+                    playerAttacker.getInventory().placeItemBackInInventory(new ItemStack(ModItems.SHADOW.get()));
+                }
+            }
+        }
+        return super.hurtEnemy(stack, target, attacker);
+    }
+
+    @Override
+    public boolean mineBlock(ItemStack stack, Level level, BlockState state, BlockPos pos, LivingEntity entityLiving) {
+        if (!level.isClientSide()) {
+            stack.hurtAndBreak(1, entityLiving, EquipmentSlot.MAINHAND);
+        }
+        Block block = state.getBlock();
+        if (!state.is(BlockTags.LEAVES) && block != Blocks.COBWEB && block != Blocks.SHORT_GRASS && block != Blocks.FERN && block != Blocks.DEAD_BUSH && block != Blocks.HANGING_ROOTS && block != Blocks.VINE && block != Blocks.TRIPWIRE && !state.is(BlockTags.WOOL)) {
+            return super.mineBlock(stack, level, state, pos, entityLiving);
+        }
+        return true;
+    }
+
+    @Override
+    public float getDestroySpeed(ItemStack stack, BlockState state) {
+        Block block = state.getBlock();
+        if (block != Blocks.COBWEB && !state.is(BlockTags.LEAVES)) {
+            return state.is(BlockTags.WOOL) ? 5.0F : super.getDestroySpeed(stack, state);
+        } else {
+            return 15.0F;
+        }
+    }
+
+    @Override
+    public InteractionResult interactLivingEntity(ItemStack stack, Player playerIn, LivingEntity entity, InteractionHand hand) {
+        if (entity.level().isClientSide) {
+            return InteractionResult.PASS;
+        } else if (entity instanceof IForgeShearable target) {
+            BlockPos pos = entity.blockPosition();
+            if (target.isShearable(stack, entity.level(), pos)) {
+                List<ItemStack> drops = target.onSheared(playerIn, stack, entity.level(), pos, stack.getEnchantmentLevel(playerIn.level().registryAccess().registryOrThrow(net.minecraft.core.registries.Registries.ENCHANTMENT).getHolderOrThrow(Enchantments.FORTUNE)));
+                Random rand = new Random();
+                drops.forEach(d -> {
+                    ItemEntity ent = entity.spawnAtLocation(d, 1.0F);
+                    if (ent != null) {
+                        ent.setDeltaMovement(ent.getDeltaMovement().add((rand.nextFloat() - rand.nextFloat()) * 0.1F, rand.nextFloat() * 0.05F, (rand.nextFloat() - rand.nextFloat()) * 0.1F));
+                    }
+                });
+                stack.hurtAndBreak(1, entity, EquipmentSlot.MAINHAND);
+            }
+            return InteractionResult.SUCCESS;
+        } else {
+            return InteractionResult.PASS;
+        }
+    }
 }
