--- src/main/java/xyz/pixelatedw/mineminenomi/events/CombatEvents.java
+++ src/main/java/xyz/pixelatedw/mineminenomi/events/CombatEvents.java
@@ -52,20 +52,20 @@
                     // Does Fishman need damage modifications?
+
+                    // Hasshoken damage modifier and pushback
+                    if (attackerStats.isAbilityActive("mineminenomi:hasshoken_passive_bonuses")) {
+                        if (attacker.getMainHandItem().isEmpty()) {
+                             event.getContainer().addModifier(
+                                 new net.neoforged.neoforge.common.damagesource.DamageModifier(net.minecraft.resources.ResourceLocation.fromNamespaceAndPath("mineminenomi", "hasshoken_damage"), 7.0F, net.neoforged.neoforge.common.damagesource.DamageModifier.Operation.ADD_VALUE)
+                             );
+                             entity.push(entity.position().subtract(attacker.position()).normalize().scale(0.6).add(0, 0.5, 0));
+                        }
+                    }
                 }

-                // Hasshoken damage modifier and pushback
-                if (stats.isAbilityActive("mineminenomi:hasshoken_passive_bonuses")) {
-                    // Emulate "HitTriggerComponent" logic (unarmed/empty hand check could go here)
-                    if (entity.getMainHandItem().isEmpty()) {
-                         // Add ability damage
-                         event.getContainer().addModifier(net.neoforged.neoforge.common.damagesource.DamageContainer.Reduction.ARMOR, (container, damage) -> damage - 7.0F); // "does not override modify(DamageContainer,float) in IReductionFunction" -> It expects modify(container, float)!
-                         attacker.push(attacker.position().subtract(entity.position()).normalize().scale(0.6).add(0, 0.5, 0));
-                    }
-                }
-
                 // Hasshoken taken damage modifier (guard block)
-                if (attackerStats != null && attackerStats.isAbilityActive("mineminenomi:hasshoken_passive_bonuses")) {
+                if (stats.isAbilityActive("mineminenomi:hasshoken_passive_bonuses")) {
                     if (entity.isBlocking()) {
-                        entity.push(entity.position().subtract(attacker.position()).normalize().scale(0.6).add(0, 0.5, 0));
+                        attacker.push(attacker.position().subtract(entity.position()).normalize().scale(0.6).add(0, 0.5, 0));
                     }
                 }
